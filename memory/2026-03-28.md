- Identified API spend leak: the expensive path was OpenClaw's built-in system heartbeat injecting reminder work into the main session, not live `cron/jobs.json`.
- Added guard in heartbeat.py to exit before API calls if OPENCLAW_HEARTBEAT_ENABLED=0 or ~/.openclaw/cron/heartbeat.disabled exists; those controls only short-circuit direct script launches.
- Persistent fix applied: set `agents.defaults.heartbeat.every = "0m"`, ran `openclaw system heartbeat disable`, and restarted the gateway.
